#include <iostream>
#define endl '\n'

#include <vector>
#include <string>
#include <algorithm>
#include <cctype>
#include <numeric>

#include <array>

using namespace std;

constexpr int board_size = 5;

class Maze2D
{
    void RotateRight()
    {
        
    }
public:
    array<array<int,board_size>,board_size> maze;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    return 0;
}